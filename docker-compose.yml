services:
  api:
    image: python:3.11-slim
    working_dir: /workspace
    command: bash -lc "pip install -e . && uvicorn agentic_research_copilot.server:app --host 0.0.0.0 --port 8000 --reload"
    ports:
      - "8000:8000"
    volumes:
      - ./:/workspace

